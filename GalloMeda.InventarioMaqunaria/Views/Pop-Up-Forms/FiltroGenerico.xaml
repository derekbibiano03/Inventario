<Window x:Class="Inventario.Desktop.Views.Pop_Up_Forms.FiltroGenerico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inventario.Desktop.Views.Pop_Up_Forms"
        mc:Ignorable="d"
        Title="FiltroGenerico" Height="450" Width="800">
    <Grid Margin="15">
      <!--
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTituloFiltro" Grid.Row="0" Text="Filtrar por:" FontSize="14" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <TextBox x:Name="TxtBuscadorInterno" Height="28" VerticalContentAlignment="Center" Padding="22,0,5,0" TextChanged="TxtBuscadorInterno_TextChanged"/>
            <TextBlock Text="🔍" Foreground="Gray" VerticalAlignment="Center" Margin="5,0,0,0" IsHitTestVisible="False"/>
        </Grid>

        <Border Grid.Row="2" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="PanelElementos">
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancelar" Width="80" Height="28" Margin="0,0,10,0" Click="BtnCancelar_Click" Background="#E0E0E0" BorderBrush="#CCCCCC"/>
            <Button Content="Aplicar" Width="80" Height="28" Click="BtnAplicar_Click" Background="#007ACC" Foreground="White" BorderThickness="0"/>
        </StackPanel>
        -->
    </Grid>
</Window>
