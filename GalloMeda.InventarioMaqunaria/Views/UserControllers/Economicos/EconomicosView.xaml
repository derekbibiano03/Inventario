<UserControl x:Class="Inventario.Desktop.Views.UserControllers.Economicos.EconomicosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventario.Desktop.Views.UserControllers.Economicos"
             xmlns:helpers="clr-namespace:Inventario.Desktop.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <helpers:UpperCaseConverter x:Key="MyUpperCaseConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Limpiar Filtros" 
                    Command="{Binding LimpiarFiltrosCommand}" 
                    Width="100" Height="28" Margin="0,14,0,0" Background="#E0E0E0" BorderBrush="#CCCCCC"/>
            <Button Content="Exportar Todo a Excel" 
                Command="{Binding ExportarExcelCommand}" 
                Width="150" 
                Height="35" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="30,10,10,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding VistaEconomicos}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  Background="White"
                  BorderBrush="#CCCCCC"
                  BorderThickness="1"
                  RowHeight="30"
                  AlternatingRowBackground="#F9F9F9">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding IdEconomico}" Width="auto">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="ID" HorizontalAlignment="Center" FontWeight="Bold"/>

                                <ComboBox Width="100" Margin="0,2,0,0"
                          ItemsSource="{Binding DataContext.FiltroIdOpciones, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          SelectedValuePath="Id">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Nombre}" 
                                      IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTextColumn Width="Auto" Binding="{Binding Descripcion}" IsReadOnly="True">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Descripción" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <ComboBox Width="150" 
                          ItemsSource="{Binding DataContext.FiltroDescripcionesOpciones, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          SelectedValuePath="Nombre">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Nombre}" 
                                      IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTextColumn Width="Auto" Binding="{Binding NombreMarca}" IsReadOnly="True">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Marca" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <ComboBox Width="100" 
                          ItemsSource="{Binding DataContext.FiltroMarcasOpciones, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          SelectedValuePath="Id">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Nombre}" 
                                      IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Modelo"  Width="Auto" Binding="{Binding Modelo}" IsReadOnly="True"/>

                <DataGridTextColumn Width="Auto" Binding="{Binding Serie}" IsReadOnly="True">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="No.Serie" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <ComboBox Width="100" 
          ItemsSource="{Binding DataContext.FiltroSeriesOpciones, RelativeSource={RelativeSource AncestorType=UserControl}}"
          SelectedValuePath="Nombre">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Nombre}" 
                      IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Año de Fabricación" Width="Auto" Binding="{Binding PeriodoFabricacion}" IsReadOnly="True"/>

                <DataGridTextColumn Width="120" Binding="{Binding IdTipoEquipoNavigation.DescripcionTipoEquipo}" IsReadOnly="True">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Tipo de Equipo" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <ComboBox Width="100" 
                          ItemsSource="{Binding DataContext.FiltroTipoEquipoOpciones, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          SelectedValuePath="Nombre">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Nombre}" 
                                      IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTextColumn Width="150" Binding="{Binding IdUbicacionNavigation.NombreProyecto}" IsReadOnly="True">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Ubicación" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <ComboBox Width="130" 
                          ItemsSource="{Binding DataContext.FiltroUbicacionesOpciones, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          SelectedValuePath="Id">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding Nombre}" 
                                      IsChecked="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>

                <DataGridTemplateColumn  Header="Acciones">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ver Detalle" 
                                    Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding IdEconomico}"
                                    Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>