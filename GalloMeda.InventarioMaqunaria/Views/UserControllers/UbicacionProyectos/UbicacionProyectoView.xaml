<UserControl x:Class="Inventario.Desktop.Views.UserControllers.UbicacionProyectos.UbicacionProyectoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventario.Desktop.Views.UserControllers.UbicacionProyectos"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Limpiar Filtros" Width="100" Height="28" Margin="0,14,0,0" Background="#E0E0E0" BorderBrush="#CCCCCC"/>
        </StackPanel>

        <DataGrid Grid.Row="1" 
          ItemsSource="{Binding Ubicaciones}" 
          AutoGenerateColumns="False" 
          CanUserAddRows="False" 
          HeadersVisibility="Column"
          GridLinesVisibility="Horizontal"
          Background="White"
          BorderBrush="#CCCCCC"
          BorderThickness="1"
          RowHeight="30"
          AlternatingRowBackground="#F9F9F9">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID del Proyecto" Width="Auto"  Binding="{Binding IdUbicacion}" IsReadOnly="True">
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="CharacterCasing" Value="Upper" />
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Nombre del Proyecto" Width="*" Binding="{Binding NombreProyecto}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ubicacion" Width="120" Binding="{Binding Ubicacion}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
